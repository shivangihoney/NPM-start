{
  "name": "npmcoursera",
  "version": "1.0.0",
  "description": "getting started with npm",
  "main": "index.html",
  "scripts": {
    "start":"set PORT = 3333 && npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite":"lite-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
